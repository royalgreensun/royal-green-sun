const menu=document.querySelector('.menu');const links=document.querySelector('.links');if(menu){menu.addEventListener('click',()=>links.classList.toggle('open'));document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')))}
const form=document.querySelector('#enquiryForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('Royal Green Sun Enquiry — '+data.get('interest'));const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nPhone / WhatsApp: ${data.get('phone')}\nInterested in: ${data.get('interest')}\n\nRequirement:\n${data.get('message')}`);window.location.href=`mailto:royal.green.sun.renewable.energy@gmail.com?subject=${subject}&body=${body}`;});}
